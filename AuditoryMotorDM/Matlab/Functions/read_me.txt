important functions
get_trajectory_metrics: calls all functions and returns the important metric values to ExampleCodeForPaper.m
preprocess_data_one_session: remove redundant vectors and divide into individual trajectories so that we can later calculate metrics
create_bin_edges.m: calculate the bin edges for the joystick workspace
compute_trajectory_metrics.m: calculate area visited, velocities, tortuosities, mean angular deviations